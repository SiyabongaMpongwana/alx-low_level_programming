#include "main.h"
/**
 * _abs - function computes absolute value of integer
 * @a: absolute value of integer
 *
 * Return: returns an integer value
 */
int _abs(int a)
{
if (a >= 0)
{
a = a;
}
else
{
a *= -1;
}
return (a);
}
